name: CI/CD Build and Push

on:
  push:
    branches:
      - development
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DockerHub (only on main branch)
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build & (Push) Backend Image
        uses: docker/build-push-action@v5
        with:
          context: project/backend
          file: project/backend/docker/Dockerfile
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ${{ secrets.DOCKER_USERNAME }}/bootcamp-devops-kresna-backend:v2
          platforms: linux/amd64

      - name: Build & (Push) Frontend Image
        uses: docker/build-push-action@v5
        with:
          context: project/frontend
          file: project/frontend/docker/Dockerfile
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ${{ secrets.DOCKER_USERNAME }}/bootcamp-devops-kresna-frontend:v2
          platforms: linux/amd64
